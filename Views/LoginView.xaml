<Window x:Class="Exercise.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đăng nhập hệ thống" Height="250" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center">

            <TextBlock Text="Tên đăng nhập:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Username}" Height="25" Margin="0,0,0,15"/>
            <TextBlock Text="Mật khẩu:" Margin="0,0,0,5"/>
            <PasswordBox Name="txtPassword" Height="25" Margin="0,0,0,15"/>

            <Button Content="Đăng nhập" 
                    Height="30" 
                    Command="{Binding LoginCommand}" 
                    CommandParameter="{Binding ElementName=txtPassword}"
                    Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>